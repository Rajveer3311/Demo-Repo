({
    doInit : function(component, event, helper) {
      //  var getMyFlow = component.find('myFlow');
        var recordId = '0015g00000R6AKdAAN';
        console.log(recordId);
        var inputVariable = [{
            Name : "StoreAccountId",
            type : "String",
            Value : recordId
        }];
   //     getMyFlow.startFlow('Display_Order_On_Account', inputVariable);
     }
})